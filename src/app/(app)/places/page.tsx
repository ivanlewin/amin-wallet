import { PlaceholderPage } from "@/app/(app)/_components/placeholder-page";

export default function PlacesPage() {
  return (
    <PlaceholderPage
      title="Places"
      description="Places are visible in the scaffold as a future concept, but they are not SQL-backed yet."
    />
  );
}
