import { PlaceholderPage } from "@/app/(app)/_components/placeholder-page";

export default function CategoriesPage() {
  return (
    <PlaceholderPage
      title="Categories"
      description="System and custom categories already exist in the data model. We will shape the UI here after wallets."
    />
  );
}
