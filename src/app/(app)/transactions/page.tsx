import { PlaceholderPage } from "@/app/(app)/_components/placeholder-page";

export default function TransactionsPage() {
  return (
    <PlaceholderPage
      title="Transactions"
      description="This route is ready to receive the same server-rendered CRUD pattern once the wallet flow feels right."
    />
  );
}
