import { PlaceholderPage } from "@/app/(app)/_components/placeholder-page";

export default function RecurrencesPage() {
  return (
    <PlaceholderPage
      title="Recurrences"
      description="Recurring transactions will live here once we replicate the wallets slice for scheduled money flows."
    />
  );
}
