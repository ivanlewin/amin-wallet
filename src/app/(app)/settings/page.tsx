import { PlaceholderPage } from "@/app/(app)/_components/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Settings is routed and scaffolded now so we can attach app-level controls later without changing the overall shell."
    />
  );
}
