import { PlaceholderPage } from "@/app/(app)/_components/placeholder-page";

export default function EventsPage() {
  return (
    <PlaceholderPage
      title="Events"
      description="Event tracking has a route and a place in the scaffold, but the operational UI is still ahead of us."
    />
  );
}
