import { PlaceholderPage } from "@/app/(app)/_components/placeholder-page";

export default function TransfersPage() {
  return (
    <PlaceholderPage
      title="Transfers"
      description="Transfers are part of the planned navigation now, even though their CRUD page will come in a later slice."
    />
  );
}
