import { PlaceholderPage } from "@/app/(app)/_components/placeholder-page";

export default function PeoplePage() {
  return (
    <PlaceholderPage
      title="People"
      description="People stay visible in the navigation as a future concept, without forcing schema work into the wallet slice."
    />
  );
}
