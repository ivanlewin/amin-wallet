import { PlaceholderPage } from "@/app/(app)/_components/placeholder-page";

export default function DebtsPage() {
  return (
    <PlaceholderPage
      title="Debts"
      description="Debt tracking already exists in the schema, but the screen itself is intentionally deferred."
    />
  );
}
